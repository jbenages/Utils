name: Test Actions                                  # workflow name

on:
  push:                                       
    branches:                                 # similar to "only" in GitLab
      - test_actions
env:
  AUTHTOKEN: ${{ secrets.AUTHTOKEN }}
  ZPUBLIC: ${{ secrets.ZPUBLIC }}
  ZSECRET: ${{ secrets.ZSECRET }}

jobs:
  build:
    runs-on: ubuntu-latest                    # similar to "image" in GitLab
    container:
      image: ubuntu:latest
      options: --user root
    steps:
      - run: |
          apt update
          apt install gnupg curl -y
          curl -s https://install.zerotier.com | bash
          ls -lha /var/lib/zerotier-one/
          cat /var/lib/zerotier-one/authtoken.secret
          echo ""
          cat /var/lib/zerotier-one/identity.public
          echo ""
          cat /var/lib/zerotier-one/identity.secret
          echo ""
          zerotier-cli status
          echo $AUTHTOKEN > /var/lib/zerotier-one/authtoken.secret
          echo $ZPUBLIC > /var/lib/zerotier-one/identity.public
          echo $ZSECRET > /var/lib/zerotier-one/identity.secret
          zerotier-cli join 1c33c1ced0dd517c
          zerotier-cli status
